# inishal
loadkeys uk
# ethernet
ping 8.8.8.8
timedatectl
# formating
lsblk -f
X fdisk -l
# make partition table
cfdisk    # use gbt
    # 4-32GB swop (ram size)
    # 1GB /boot
    # everything else file system
mkfs.ext4 /dev/sda3        # make root
mkfs.vfat -F 32 /dev/sda1        # make file system
mkswap /dev/sda2        # make swap
# mount
mount /dev/sda3 /mnt            # root
mount --mkdir /dev/sda1 /mnt/boot    # make and mount boot
swapon /dev/sda2            # set the swap
# mirrors
pacstrap -K /mnt base linux linux-firmware     # bace
intel-ucode        # CPU
nvidia-open        # GPU
networkmanager        # ez wifi
nano            # text
man-db man-pages    # manual
texinfo            # not manual but similar
grub efibootmgr        # boot loader
hyprland        # interface
# all together
pacstrap -K /mnt base linux linux-firmware intel-ucode nvidia-open nano man-db man-pages texinfo grub efibootmgr 
# idk
genfstab -U /mnt >> /mnt/etc/fstab            # funny line useful, find out later
arch-chroot /mnt                    # actualy mount root
ln -sf /usr/share/zoneinfo/Europe/London /etc/localtime    # time zone
hwclock --systohc                    # update clock
passwd                            # it's password why are you reading this
# GRUB boot loader
X mount /dev/sda1 /mnt/boot    # mount again
grub-install --target=x86_64-efi --efi-directory=/BOOT --bootloader-id=GRUB
# When finnished
exit
reboot

#On next boot up
pacman -S hyprland 

look at fuck